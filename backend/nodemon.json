{
  "watch": ["src"],
  "ext": "ts, js, graphql, json",
  "ignore": ["node_modules", "coverage", "dist"],
  "exec": "ts-node -r tsconfig-paths/register ./src/app.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
